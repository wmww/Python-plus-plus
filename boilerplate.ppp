#include "ppp.ppp"
exec(open("ppp.ppp").read())

def run(args):
	
	var
	a = Array(0);
	
	a.append(9);
	a.append(12);
	a.append(3);
	
	for i in (a):
		print(i);
	end
	
	for i in (range(0, 10)):
		print(i);
	end
	#define comment //for i in range(100):
	#define comment	//print(i)
	#define comment //end
end

END(run(_));

