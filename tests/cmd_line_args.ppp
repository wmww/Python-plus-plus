#define comment // $ abc -m 8
#define comment // > 3
#define comment // > abc
#define comment // > -m
#define comment // > 8

#include "ppp"
exec(open("ppp").read())

def run(args):
	print(len(cmd_line_args));
	if (len(cmd_line_args) >= 1):
		print(cmd_line_args[0]);
	end
end

end_program()
